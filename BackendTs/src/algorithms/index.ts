import logger from "../config/logger";

export const exampleAlgorithm = (data: any) => {
    logger.info("Running example algorithm");
    // Thêm logic thuật toán xử lý dữ liệu
    return data;
};